{
  "name": "jdbc_postgres_connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "connection.url": "jdbc:postgresql://localhost/egor_database",
    "connection.user": "egorka",
    "connection.password": "egorka",
    "topic.prefix": "psql-egor-",
    "mode": "bulk",
    "table.whitelist": "products"
  }
}
